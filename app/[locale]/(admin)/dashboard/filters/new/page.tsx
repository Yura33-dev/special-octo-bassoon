import NewFilterPage from '../../../_components/filters-page/new-page/NewFilterPage';

export const metadata = {
  title: 'ProGround | Новий фільтр',
};

export default function Page() {
  return <NewFilterPage />;
}
